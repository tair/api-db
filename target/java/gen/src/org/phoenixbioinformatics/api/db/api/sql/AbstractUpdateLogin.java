/**
 * Copyright 2011 Carnegie Institution for Science. All rights reserved.
 */
// Generated by AndroMDA Poesys/DB cartridge: DO NOT EDIT DIRECTLY.
// Template: AbstractUpdate.vsl

package org.phoenixbioinformatics.api.db.api.sql;


import java.sql.PreparedStatement;
import java.sql.SQLException;

import com.poesys.db.dao.update.IUpdateSql;
import com.poesys.db.pk.IPrimaryKey;


/**
 * SQL statement specification for updating a Login with read/write properties
 * 
 * @author Robert J. Muller
 */
public class AbstractUpdateLogin implements IUpdateSql<org.phoenixbioinformatics.api.db.api.ILogin> {
  /** SQL UPDATE statement for Login */
  private static final String SQL =
    "UPDATE Login SET password = ?, email = ?, institution = ?, userIdentifier = ?, partnerId = ? WHERE ";

  @Override
  public String getSql(IPrimaryKey key) {
    StringBuilder builder = new StringBuilder(SQL);
    builder.append(key.getSqlWhereExpression(""));
    return builder.toString();
  }

  @Override
  public int setParams(PreparedStatement stmt, int index, org.phoenixbioinformatics.api.db.api.ILogin object)
      throws SQLException {
    stmt.setString(index, object.getPassword());
    index++;
    stmt.setString(index, object.getEmail());
    index++;
    stmt.setString(index, object.getInstitution());
    index++;
    stmt.setString(index, object.getUserIdentifier());
    index++;
    // sets key params for required partner object
    index = object.getPartner().getPrimaryKey().setParams(stmt, index);
    // sets primary key in where clause
    index = object.getPrimaryKey().setParams(stmt, index);
    return index;
  }
}
